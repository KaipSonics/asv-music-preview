# Пример переменных окружения. Скопируй в .env.local и впиши реальные значения.
# .env.local в git НЕ попадает (см. .gitignore). Реальные токены задаём в Railway → Variables.

# Какой провайдер генерации использовать: huggingface (бесплатно) или replicate (платно)
AUDIO_PROVIDER=huggingface

# Токен Hugging Face — https://huggingface.co/settings/tokens (бесплатный, тип "Read")
HF_TOKEN=hf_xxxxxxxxxxxxxxxxxxxxxxxx

# (опционально) другая модель Hugging Face
# HF_MODEL=facebook/musicgen-small

# Нужно только если AUDIO_PROVIDER=replicate
# REPLICATE_API_TOKEN=r8_xxxxxxxxxxxxxxxxxxxxxxxx
